{
  "name": "pod",
  "version": "0.1.0",
  "description": "Type A Machines Pod interface.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm run build",
    "clean": "rm -rf build/* && mkdir -p build/css && mkdir -p build/js",
    "assets": "ln -s ./client/assets build/assets",
    "optimize:images": "echo 'TODO",
    "watch": "webpack --config webpack.config.js -d --progress --profile --colors --watch",
    "build-js": "webpack --config webpack.config.js -p --progress --profile --colors",
    "build-js-dev": "webpack --config webpack.config.js -d --progress --profile --colors",
    "browser-sync": "browser-sync start --config browsersync.config.js",
    "start": "npm run build && npm run watch & npm run browser-sync",
    "build": "npm run clean && npm run build-js ",
    "build-dev": "npm run clean && npm run build-js-dev && npm run postinstall"
  },
  "repository": {
    "type": "git",
    "url": " "
  },
  "author": "Type A Machines",
  "license": "UNLICENSED",
  "dependencies": {
    "bootstrap": "^3.3.5",
    "react": "^0.14.1",
    "react-dom": "^0.14.1"
  },
  "devDependencies": {
    "babel": "^6.0.2",
    "babel-loader": "^5.3.3",
    "node-sass": "^3.4.1",
    "webpack": "^1.12.2"
  }
}
