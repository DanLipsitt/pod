{
  "name": "pod",
  "version": "0.1.0",
  "description": "Type A Machines Pod interface.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm run build",
    "clean": "rm -rf build/* && mkdir -p build/css && mkdir -p build/js",
    "assets": "ln -s ./client/assets build/assets",
    "optimize:images": "echo 'TODO",
    "watch-js": "watchify client/js/app.js -o build/js/app.js --debug",
    "build-js": "browserify client/js/app.js | uglifyjs -m -c warnings=false > build/js/app.js",
    "build-js-dev": "browserify client/js/app.js -o client/js/app.js --debug",
    "watch-sass": "node-sass client/scss -o build/css --source-map=true --source-map-contents=true --source-map-embed=true -w",
    "watch-sass-autoprefixer": "node-sass client/scss -o build/tmp/css && node-sass client/scss -o build/tmp/css --source-map=true --source-map-contents=true --source-map-embed=true -w & postcss --use autoprefixer --autoprefixer.browsers 'last 2 versions' -d build/css/ build/tmp/css/*.css -w",
    "build-sass": "node-sass client/scss --output-style compressed -o build/css",
    "build-sass-dev": "node-sass client/scss -o build/css --source-map=true --source-map-contents=true --source-map-embed=true",
    "build-autoprefixer": "postcss --use autoprefixer --autoprefixer.browsers 'last 2 versions' -d build/css/ build/css/*.css",
    "start-sass": "npm run watch-sass & npm run browser-sync",
    "start-sass-autoprefixer": "npm run watch-sass-autoprefixer & npm run browser-sync",
    "browser-sync": "browser-sync start --config browsersync.config.js",
    "start": "npm run build && npm run watch-js & npm run watch-sass & npm run browser-sync",
    "build": "npm run clean &&  npm run build-sass && npm run build-js && npm run build-autoprefixer",
    "build-dev": "npm run clean && npm run build-sass-dev && npm run build-js-dev && npm run build-autoprefixer && npm run postinstall"
  },
  "repository": {
    "type": "git",
    "url": " "
  },
  "author": "Type A Machines",
  "license": "UNLICENSED",
  "dependencies": {
    "bootstrap": "^3.3.5",
    "react": "^0.14.1",
    "react-dom": "^0.14.1"
  },
  "devDependencies": {
    "babel": "^6.0.2",
    "node-sass": "^3.4.1",
    "webpack": "^1.12.2"
  }
}
